<header class="topnav-container">
  <nav class="topnav" aria-label="Main navigation">
    <ul class="nav-list">
      <li><a href="/about" class="nav-link"><b>About</b></a></li>
      <li><a href="/blog" class="nav-link"><b>Blog</b></a></li>
      <li><a href="/contacts" class="nav-link"><b>Contacts</b></a></li>
    </ul>
  </nav>
</header>

<style>
  .topnav-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    padding: 0.9rem 1rem 0;
    box-sizing: border-box;
  }

  .topnav {
    display: flex;
    justify-content: center;
    padding: 0.75rem 1rem;
    margin: 0 auto;
    border: 1px solid rgba(var(--accent-light), 0.16);
    border-radius: 999px;
    max-width: 860px;
    background:
      linear-gradient(135deg, rgba(var(--accent), 0.22), rgba(var(--button-background), 0.94)),
      rgba(var(--button-background), 0.88);
    box-shadow:
      0 18px 40px rgba(0, 0, 0, 0.32),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(18px);
  }

  .nav-list {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    width: 100%;
    list-style-type: none;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 7rem;
    padding: 0.8rem 1.35rem;
    border-radius: 999px;
    color: rgb(var(--accent-light));
    text-decoration: none;
    font-size: clamp(0.95rem, 1vw + 0.6rem, 1.15rem);
    font-weight: 600;
    letter-spacing: 0.02em;
    transition:
      color 0.25s ease,
      transform 0.25s ease,
      background-color 0.25s ease,
      box-shadow 0.25s ease;
  }

  .nav-link:hover {
    color: rgb(var(--accent-light-hover));
    background: rgba(var(--accent-light), 0.12);
    box-shadow: inset 0 0 0 1px rgba(var(--accent-light), 0.12);
    transform: translateY(-1px);
    text-decoration: none;
  }

  .nav-link:focus-visible {
    outline: 2px solid rgba(var(--accent-light-hover), 0.9);
    outline-offset: 3px;
  }

  @media (max-width: 640px) {
    .topnav-container {
      padding-top: 0.75rem;
    }

    .topnav {
      border-radius: 24px;
      padding: 0.65rem 0.75rem;
    }

    .nav-list {
      flex-wrap: wrap;
      gap: 0.35rem;
    }

    .nav-link {
      min-width: 0;
      padding: 0.65rem 0.85rem;
      font-size: 0.9rem;
    }
  }
</style>
